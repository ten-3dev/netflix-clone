import { SafeAreaView } from "react-native";

import styled from "styled-components";

//common style

const CosSafeAreaView = styled(SafeAreaView)`
    background-color: black;
    flex: 1;
`;

export {CosSafeAreaView};